import { GetParentNamePipe } from './get-parent-name.pipe';

describe('GetParentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetParentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
